offregister_databases
===============

Follows [offregister](https://github.com/offscale/offregister) specification. Currently supports:

  - Postgres (on Ubuntu 14.04 LTS)

## Install dependencies

    pip install -r requirements.txt

## Install package

    pip install .
